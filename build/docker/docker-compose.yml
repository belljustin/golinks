version: "3.9"
services:
  golinks:
    build:
      context: ../../
      dockerfile: build/docker/Dockerfile
    ports:
      - "8080:8080"
  dynamodb:
    image: "amazon/dynamodb-local:1.20.0"
    ports:
      - "8000:8000"